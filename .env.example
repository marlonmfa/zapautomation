# Session identifier: same value = same WhatsApp session (auth stored per SESSION_ID).
# Use a unique string per phone/user (e.g. "my-phone" or "user123") so you don't scan QR every time.
SESSION_ID=default

# Optional: random wait (ms) before each batch message to avoid being flagged as spam.
# Each message waits a random time between MIN and MAX. Defaults: 5000–30000 ms (5–30 s).
# BATCH_DELAY_MIN_MS=5000
# BATCH_DELAY_MAX_MS=30000

# Optional: max wait (ms) per send; if a send hangs, batch continues after this. Default: 60000 (60 s).
# BATCH_SEND_TIMEOUT_MS=60000

# Optional: when false, send to ALL contacts (do not skip those who already received). Default: true.
# Use BATCH_SKIP_IF_EVER_SENT=false to force send to everyone (e.g. new list, re-send).
# BATCH_SKIP_IF_EVER_SENT=false

# Optional: when false, use WhatsApp API send instead of browser (simulate typing). Default: true.
# Use BATCH_USE_BROWSER_SEND=false if browser send fails.
# BATCH_USE_BROWSER_SEND=false

# Optional: path where session data is stored (relative to project root). Default: .wwebjs_auth
# AUTH_DATA_PATH=.wwebjs_auth

# Optional: path to Chrome/Chromium so Puppeteer uses it (avoids "Could not find Chrome").
# macOS example:
# PUPPETEER_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# WA links server (serve webpage with wa.me links; no WhatsApp client).
# BATCH_FILE=path/to/batch.json
# LINKS_SERVER_PORT=3456

# Reply suggestion + audio transcription (when ENABLE_REPLY_SUGGESTION=true).
# Get your key at https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...
# ENABLE_REPLY_SUGGESTION=true

# Voice samples for TTS/synthesizer: save incoming audio from this number when running listen:voice-samples.
# Normalized digits only, e.g. 5547988685743 for +55 47 98868-5743. Default: 5547988685743
# VOICE_SAMPLES_PHONE=5547988685743

# Text-to-speech and voice cloning (send voice messages to WhatsApp).
# Get your key at https://elevenlabs.io/app/settings/api-keys
# ELEVENLABS_API_KEY=...
